{
  "name": "etzhayyim-wasm-browser-br0ws3r1",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd svelte && pnpm install --frozen-lockfile && pnpm build",
    "dev": "cd svelte && pnpm dev",
    "deploy": "pnpm build && wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.99.0"
  }
}
